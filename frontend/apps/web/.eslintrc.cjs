module.exports = {
  extends: [require.resolve("@support-me/config/eslint-preset")],
  // Next.js auto-generates/regenerates this file; it's not meant to be
  // edited or linted (its triple-slash references are Next's own convention).
  ignorePatterns: ["next-env.d.ts"],
};
