# Forces http:// -> https:// at the load balancer, before traffic ever reaches a pod.
apiVersion: networking.gke.io/v1beta1
kind: FrontendConfig
metadata:
  name: support-me-frontend-config
  namespace: support-me
spec:
  redirectToHttps:
    enabled: true
    responseCodeName: MOVED_PERMANENTLY_DEFAULT
